export const initState = {
    apiObj: {},
    isFetching: false,
    err: ""
}

const apodReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default apodReducer;